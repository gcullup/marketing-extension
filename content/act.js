// The only file allowed to click or type. Nothing here performs a real
// action yet — Phase 1 (task 1.9) adds the assisted-click Add Friend flow,
// gated by Test Mode (a no-op dry run) until it's proven safe.

(function () {
  const MKT = (self.MKT ||= {});
  MKT.act = {
    // TODO Phase 1: clickAddFriend(candidateId), respecting Test Mode.
  };
})();
